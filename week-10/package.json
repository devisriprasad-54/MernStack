{
  "name": "mernstack-blog",
  "version": "1.0.0",
  "description": "Full-stack blog platform",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev -w Frontend\" \"npm run dev -w Backend\"",
    "install-all": "npm install && npm install -w Backend && npm install -w Frontend",
    "build": "npm install -w Backend && npm install -w Frontend && npm run build -w Frontend",
    "start": "node Backend/server.js"
  },
  "workspaces": [
    "Backend",
    "Frontend"
  ],
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
