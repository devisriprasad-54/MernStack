#### Backend Development
1.Create git repo
    git init // Initialize git repo
2.Add .gitignore
3.Create .env file for environment variables & Read data from .env with "dotenv" module
    npm i dotenv // Install dotenv
4.Generate package.json
    npm init -y // Generate package.json
5.Create Express Application app

6.Connect to Database
    npm install mongoose // Install mongoose import connect from "mongoose"
7.Add middleware to parse JSON payloads
    app.use(express.json())
8.Design Schemas and create models

9.Design REST APIs for all resources

### Registration & Login
10. Registration & login 
    
    